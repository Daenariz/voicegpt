[project]
name = "voicegpt"
version = "0.1.0"
description = ""
authors = [
    { name = "Daenariz" }
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai ==0.28",
    "pyttsx3 >=2.98,<3.0",
    "speechrecognition >=3.12.0,<4.0",
    "python-dotenv >=1.0.1,<2.0",
    "pyaudio >=0.2.14,<0.3"
]

[build-system]
requires = ["setuptools >=67.8.0"]
build-backend = "setuptools.build_meta"
